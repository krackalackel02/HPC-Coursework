

\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{fontspec}
\usepackage[math-style=upright]{unicode-math}
\setmainfont{Calibri}
% \setmathfont{Calibri}
% \setmathfont[slash-delimiter=frac]{Cambria Math}
% \setmathfont[range=up]{Calibri}
% \setmathfont[range=it]{Calibri Italic}
% \setmathfont[range=bfup]{Calibri Bold}
% \setmathfont[range=bfit]{Calibri Bold Italic}
\setmathfont{NewCMMath-Regular.otf}
\usepackage[top=1cm,bottom=0.75cm, left=1.1cm, right=1.1cm]{geometry}
\usepackage{amsmath}
\usepackage{tabto}
\usepackage{multicol}
\usepackage{lineno, blindtext}
\usepackage[dvipsnames]{xcolor}
\usepackage{MnSymbol}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{makecell}
\usepackage{datatool}
\newcommand{\datavalue}[1]{#1}
\newcommand{\unit}[1]{~#1~}
\usepackage{nicefrac}
% \usepackage{listings}
% \lstset{language=Matlab,
%     basicstyle=\ttfamily,
%     keywordstyle=\color{blue},
%     commentstyle=\color{green!60!black},
%     stringstyle=\color{red},
%     showstringspaces=false,
%     numbers=left,
%     numberstyle=\tiny\color{gray},
%     stepnumber=1,
%     numbersep=5pt,
%     tabsize=4,
%     breaklines=true,
%     breakatwhitespace=true,
%     frame=single,
%     captionpos=b,
%     belowskip=10pt
% }
% \renewcommand{\thesubsection}{Task \Roman{subsection}}
% \renewcommand{\thesection}{\underline{Task \arabic{section}}}


\pagestyle{fancy}
\fancyhf{} % Clear all header and footer fields
% \fancyfoot[R]{\textcolor{gray}{CID: 02029025}}
\fancyfoot[C]{\thepage}
% \fancyfoot[L]{\textcolor{gray}{Part 1}}
\renewcommand{\headrulewidth}{0pt} % Remove the header line
\setlength{\headheight}{0pt} % Remove the header space

\makeatletter
\def\@makechapterhead#1{%
  % Reduce space above
  \vspace*{-3.0em}%
  {\parindent \z@ \raggedright \normalfont
    \ifnum \c@secnumdepth >\m@ne
        \huge\bfseries \@chapapp\space \thechapter
        \par\nobreak
        \vskip 20\p@
    \fi
    \interlinepenalty\@M
    \Huge \bfseries #1\par\nobreak
    \vskip 40\p@
  }}
\def\@makeschapterhead#1{%
  % Reduce space above
  \vspace*{-3.0em}%
  {\parindent \z@ \raggedright
    \normalfont
    \interlinepenalty\@M
    \Huge \bfseries  #1\par\nobreak
    \vskip 40\p@
  }}
\makeatother
\makeatletter
\renewcommand\section{\@startsection{section}{1}{\z@}%
    {-0.3\baselineskip}%  % Space before section
    {0.1\baselineskip}%    % Space after section
    {\normalfont\Large\bfseries}}
\renewcommand\subsection{\@startsection{subsection}{2}{\z@}%
    {-0.2\baselineskip}%   % Space before subsection
    {0.05\baselineskip}%    % Space after subsection
    {\normalfont\large\bfseries}}
\renewcommand\subsubsection{\@startsection{subsubsection}{3}{\z@}%
    {-0.1\baselineskip}%  % Space before subsubsection
    {0.05\baselineskip}%   % Space after subsubsection
    {\normalfont\normalsize\bfseries}}
\makeatother
% Adjust spacing for figure captions
\captionsetup[figure]{skip=-3pt}
% Adjust spacing for table captions
\captionsetup[table]{skip=-2pt}
\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt}
% \setlength{\belowcaptionskip}{-10pt}

\usepackage{csquotes}
% \usepackage[style = numeric,sorting=none,url=true,giveninits=false,backend=bibtex]{biblatex}
% \newbibmacro{string+doiurlisbn}[1]{%
%   \iffieldundef{doi}{%
%     \iffieldundef{url}{%
%       \iffieldundef{isbn}{%
%         \iffieldundef{issn}{%
%           #1%
%         }{%
%           \href{http://books.google.com/books?vid=ISSN\thefield{issn}}{#1}%
%         }%
%       }{%
%         \href{http://books.google.com/books?vid=ISBN\thefield{isbn}}{#1}%
%       }%
%     }{%
%       \href{\thefield{url}}{#1}%
%     }%
%   }{%
%     \href{http://dx.doi.org/\thefield{doi}}{#1}%
%   }%
% }
% \DeclareFieldFormat{title}{\usebibmacro{string+doiurlisbn}{\mkbibemph{#1}}}
% \DeclareFieldFormat[article,incollection]{title}%
%     {\usebibmacro{string+doiurlisbn}{\mkbibquote{#1}}}




% \addbibresource{biblio/export.bib}


% \def\runit{$\unit{km}$}
% \def\vunit{$\unit{km} \unit{s^{-1}}$}
% \def\muunit{$\unit{km^3} \unit{s^{-2}}$}
% \def\hunit{$\unit{km^2} \unit{s^{-1}}$}
% \def\degunit{$\unit{^{\circ}}$}
% \def\radunit{$\unit{rad}$}
% \def\timeunit{$\unit{hours}$}


\setcounter{secnumdepth}{3} % depth level 3 corresponds to subsubsections
\usepackage[title,titletoc]{appendix}
\usepackage{matlab-prettifier}
\usepackage{pdfpages}
\usepackage{etoolbox}
\usepackage{comment}
\usepackage{titlesec}
\usepackage{listings}
\lstdefinestyle{myCppStyle}{
  language=C++, % Syntax highlighting for C++
  basicstyle=\ttfamily\small, % Use a small, monospaced font for code
  keywordstyle=\color{blue}, % Keywords in blue
  commentstyle=\color{green!60!black}, % Comments in dark green
  stringstyle=\color{red}, % Strings in red
  showstringspaces=false, % Don't mark spaces in strings
  numbers=left, % Line numbers on the left
  numberstyle=\tiny\color{gray}, % Line numbers in tiny, gray text
  stepnumber=1, % Line numbers step
  numbersep=5pt, % How far the line numbers are from the code
  tabsize=4, % Tab size
  breaklines=true, % Break long lines
  breakatwhitespace=false, % Break lines at whitespace
  frame=single, % Single frame around the code
  captionpos=b, % Caption position at bottom
  belowskip=10pt, % Space below the listing
  frameshape={RYR}{Y}{Y}{RYR}, % Custom frame around the listing
  upquote=true, % Use upright quotes
  aboveskip=10pt, % Space above the listing
  columns=fullflexible, % Column formatting
  escapeinside={(*@}{@*)}, % Escape to LaTeX between (*@ and @*)
}
\lstdefinestyle{myPythonStyle}{
  language=Python, % Syntax highlighting for Python
  basicstyle=\ttfamily\small, % Use a small, monospaced font for code
  keywordstyle=\color{blue}, % Keywords in blue
  commentstyle=\color{green!60!black}, % Comments in dark green
  stringstyle=\color{red}, % Strings in red
  showstringspaces=false, % Don't mark spaces in strings
  numbers=left, % Line numbers on the left
  numberstyle=\tiny\color{gray}, % Line numbers in tiny, gray text
  stepnumber=1, % Line numbers step
  numbersep=5pt, % How far the line numbers are from the code
  tabsize=4, % Tab size
  breaklines=true, % Break long lines
  breakatwhitespace=false, % Break lines at whitespace
  frame=single, % Single frame around the code
  captionpos=b, % Caption position at bottom
  belowskip=10pt, % Space below the listing
  frameshape={RYR}{Y}{Y}{RYR}, % Custom frame around the listing
  upquote=true, % Use upright quotes
  aboveskip=10pt, % Space above the listing
  columns=fullflexible, % Column formatting
  escapeinside={(*@}{@*)}, % Escape to LaTeX between (*@ and @*)
}
\lstdefinestyle{myBashStyle}{
  language=sh, % Syntax highlighting for Bash
  basicstyle=\ttfamily\small, % Use a small, monospaced font for code
  keywordstyle=\color{blue}, % Keywords in blue
  commentstyle=\color{green!60!black}, % Comments in dark green
  stringstyle=\color{red}, % Strings in red
  showstringspaces=false, % Don't mark spaces in strings
  numbers=left, % Line numbers on the left
  numberstyle=\tiny\color{gray}, % Line numbers in tiny, gray text
  stepnumber=1, % Line numbers step
  numbersep=5pt, % How far the line numbers are from the code
  tabsize=4, % Tab size
  breaklines=true, % Break long lines
  breakatwhitespace=false, % Break lines at whitespace
  frame=single, % Single frame around the code
  captionpos=b, % Caption position at bottom
  belowskip=10pt, % Space below the listing
  upquote=true, % Use upright quotes
  aboveskip=10pt, % Space above the listing
  columns=fullflexible, % Column formatting
  escapeinside={(*@}{@*)}, % Escape to LaTeX between (*@ and @*)
}
\lstset{
  stepnumber=1, % Line numbers step
  numbersep=5pt, % How far the line numbers are from the code
  tabsize=4, % Tab size
  breaklines=true, % Break long lines
  breakatwhitespace=false, % Break lines at whitespace
  frame=single, % Single frame around the code
  captionpos=b, % Caption position at bottom
  belowskip=10pt, % Space below the listing
  frameshape={RYR}{Y}{Y}{RYR}, % Custom frame around the listing
  upquote=true, % Use upright quotes
  aboveskip=10pt, % Space above the listing
  columns=fullflexible, % Column formatting
  escapeinside={(*@}{@*)}, % Escape to LaTeX between (*@ and @*)
}

% SECTION TITLE: 1
\renewcommand{\thesection}{\arabic{section}}

% SUBSECTION TITLE: 1A
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}}

% SUBSubSECTION TITLE: 1A
\renewcommand{\thesubsubsection}{\arabic{section}.\arabic{subsection}.\arabic{subsubsection}}
\usepackage{subcaption} % For subfigures

\newcommand{\eq}{Equation~}
\newcommand{\eqs}{Equations~}
\newcommand{\fig}{Figure~}
\newcommand{\figs}{Figures~}
\newcommand{\code}{Code Snippet~}
\newcommand{\codes}{Code Snippets~}
\newcommand{\tabls}{Tables~}
\newcommand{\tabl}{Table~}
\newcommand{\app}{Appendix~}

\usepackage{matlab-prettifier}
\usepackage{fancyvrb}
\usepackage{rotating}
\usepackage{verbatim}
\usepackage{multicol}
\usepackage{booktabs}
\usepackage{colortbl}
\titlespacing*{\subsection}{0pt}{0.4\baselineskip}{0.4\baselineskip}
\titlespacing*{\section}{0pt}{1\baselineskip}{0.4\baselineskip}
\usepackage{parskip} % adds spacing between paragraphs
\usepackage{indentfirst} % indents the first line of the first paragraph
\setlength{\parindent}{2em} % sets the length of the indent for subsequent paragraphs
\usepackage{minted}
\usepackage{etoolbox} % For patching environments

% Link the counters of 'listing' and 'lstlisting'
\AtBeginEnvironment{listing}{\setcounter{listing}{\value{lstlisting}}}
\captionsetup[listing]{name=Code Snippet}